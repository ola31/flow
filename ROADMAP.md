# Flow Roadmap

## 완료된 작업 (feature-new-ux)

### UX 구조 개선
- 홈 화면: 곡 라이브러리 / 프로젝트 두 패널 구조
- 셋리스트: QTreeWidget → 카드 기반 뷰 (상태 배지, 인라인 편집/제거)
- 매핑 패널: 핫스팟 선택 시 절별 매핑 현황 우측 패널
- 라이브러리 브라우저: 곡 추가 시 상태 표시 + 검색 지원 다이얼로그
- 고스트 핫스팟: 마우스 따라다니는 반투명 미리보기
- 라이브 모드: 키보드 힌트 바, 종료 버튼(Esc), 확인 팝업

### 디자인 시스템
- Material Symbols Rounded 아이콘 폰트 (66KB 서브셋)
- 5단계 그레이 + 1 액센트 컬러 팔레트 (styles.py 토큰)
- 이모지 전면 제거 → 아이콘 폰트 + 텍스트
- 과도한 볼드 제거, 배경 톤 통일

---

## 다음 단계: 워크스페이스 구조 개편

### 배경

현재 곡(Song)이 프로젝트 폴더 안에 포함되는 구조:
```
프로젝트폴더/
├── project.json
└── songs/
    ├── 곡A/
    └── 곡B/
```

**문제점:**
- 같은 곡을 여러 프로젝트에서 사용 시 복사본이 생김
- 한 프로젝트에서 매핑 수정해도 다른 프로젝트에 반영 안 됨
- 프로젝트 복제 시 곡 파일도 통째로 복제 → 디스크 낭비
- 실제 사용 패턴: 이전 프로젝트 폴더를 통째로 복사 → 이름 변경 → project.json 직접 편집

### 목표 구조

```
워크스페이스/                      ← 위치 자유, 여러 개 생성 가능
├── library/                      ← 공용 곡 라이브러리
│   ├── 곡A/
│   │   ├── song.json
│   │   ├── sheets/
│   │   └── slides.pptx
│   ├── 곡B/
│   └── ...
└── projects/
    ├── 2024-12-25 행사/
    │   ├── project.json          ← library 곡을 이름으로 참조
    │   └── songs/                ← 로컬 오버라이드 (선택적)
    │       └── 곡B/              ← 이 프로젝트만의 커스텀 버전
    └── 2025-01-05 행사2/
        ├── project.json
        └── songs/                ← 비어있으면 전부 library에서 참조
```

### 핵심 설계 원칙

1. **곡 해석 우선순위**: `projects/{name}/songs/{곡}` → 없으면 → `library/{곡}`
   - 같은 곡이 로컬에 있으면 로컬 우선 (커스텀 PPT 배경 등)
   - 없으면 공용 라이브러리에서 참조

2. **워크스페이스 자유도**:
   - 어디에든 위치 가능 (USB, 클라우드 드라이브 등)
   - 한 PC에 여러 워크스페이스 가능 (용도별, 행사별)
   - 앱 시작 시 최근 워크스페이스 기억 (ConfigService)

3. **프로젝트 복제**:
   - `project.json` 하나만 복사하면 끝 (곡 파일 복사 불필요)
   - UI에서 "복제해서 새로 만들기" 지원

4. **곡 추가 흐름**:
   - "라이브러리에서 참조 추가" — project.json에 이름만 추가
   - "로컬에 복사해서 추가" — songs/ 폴더에 복사본 생성 (커스터마이즈용)

### 변경 범위

1. **Domain**
   - `Song.folder` 경로 해석 로직 → 워크스페이스 기준 상대 경로
   - `Project`에 워크스페이스 루트 경로 참조 추가
   - song_order 항목에 source 구분 (library / local) 추가 가능

2. **Repository**
   - `ProjectRepository`: 워크스페이스 구조 인식, library/projects 분리 로딩
   - 곡 로딩 시 우선순위 적용 (local → library fallback)
   - 워크스페이스 초기화 (library/, projects/ 생성)

3. **Services**
   - `ConfigService`: 최근 워크스페이스 경로 목록 관리
   - `SlideManager`: 곡 경로 해석이 바뀌므로 슬라이드 로딩 경로 수정

4. **UI**
   - 홈 화면: 워크스페이스 선택/생성 UI 추가
   - ProjectLauncher: projects/ 폴더 내 프로젝트 목록 표시
   - SongListWidget: "라이브러리에서 참조" vs "로컬 복사" 선택
   - SongLibraryDialog: library/ 폴더 스캔
   - 프로젝트 복제 기능 (우클릭 메뉴 또는 버튼)

5. ~~마이그레이션~~ — 제거됨 (기존 프로젝트는 모두 테스트용이었고 이전할 필요 없음)
